Instalar estas dependencias en la carpeta donde tienen la práctica:
npm install express sqlite3 body-parser

Correr servidor con:
node server.js

Entrar a http://localhost:3000/login.html para ver la página
